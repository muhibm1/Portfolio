Verdict: 4 findings (0 critical, 0 high, 2 medium, 2 low)

| Severity | File:line | Finding | Failure scenario | Fix |
|---|---|---|---|---|
| medium | src/checkPhoneRedaction.test.js:283-296 | E19 ("never prints matched text...") asserts only the absence of `556`/`0100`/`today` in reportLines, never `exitCode` or a hit count (confirmed by reading the test body) | scanner regresses to finding zero hits on the mixed-encoding fixture (the exact bug this change fixes) -> reportLines contains only the "0 hits" summary line, which also contains none of `556`/`0100`/`today` -> this test stays green | src/checkPhoneRedaction.test.js: add `expect(exitCode).toBe(EXIT_HIT)` and a hit-count assertion (as E1/E2/E3 already do) to this test so it cannot pass on a silent miss |
| medium | src/checkTestFloor.test.js:102-126 | E30 asserts only the static half of the failure message, `need at least 44 passed and 0 not passed` (confirmed at line 124); it never checks the `got X passed, Y not passed` half that carries the actually computed counts | `redactionSuiteCounts` in scripts/check-test-floor.mjs miscounts (e.g. off-by-one in `notPassed`) while still failing the `>=44 && ===0` check -> stdout still contains the static substring this test checks -> the wrong-count bug ships undetected | src/checkTestFloor.test.js: add `expect(result.stdout).toContain(`got ${expectedPassed} passed, ${expectedNotPassed} not passed`)` per case in the `it.each`, with `expectedPassed`/`expectedNotPassed` derived from each fixture's `counts` |
| low | src/checkTestFloor.test.js:75-89 | E28 (admit side) checks only that a `Redaction suite ...:` line exists, not the numeric `44 passed, 0 not passed` it should print (confirmed) | a counting bug that always reports a fixed high `passed`/`notPassed` regardless of input would still print a line starting `Redaction suite ...:` and pass this specific test; the bug would only surface via other tests (E30), not here | src/checkTestFloor.test.js:88 add `expect(result.stdout).toContain(`${PINNED_REDACTION_PASSED_COUNT} passed, 0 not passed`)` |
| low | scripts/phone-redaction-scan.mjs:232-236 (worseExitCode), no test | evals.md's own matrix never forces the self-test form-check outcome to be EXIT_HIT while a directory scan is EXIT_CLEAN/EXIT_CANNOT_RUN (believed, not verified: every self-test-with-directory test in checkPhoneRedaction.test.js keeps the matcher forms correct, so the form-check branch of `worseExitCode` is always CLEAN) | a regression that inverts `worseExitCode`'s precedence (e.g. preferring CANNOT_RUN over HIT) would only be caught if a form-check failure and a directory result actually collide, which no test forces | none proposed; out of the 34-case contract, flagged as a coverage note rather than a fix against a named eval |

Findings outside scope: none.

Not verified: whether `scripts/check-test-floor.mjs` behaves correctly on a syntactically valid JSON report with an unexpected top-level shape (e.g. missing `testResults` entirely, non-array `assertionResults`) was not tested by any of E28-E33 and I did not add a test to check it; the source has defensive fallbacks (`Array.isArray(...) ? ... : []`, `Number(...) || 0`) but they are exercised only incidentally, not directly, by the existing fixtures (believed, not verified).

Summary of what does hold up (confirmed by reading both files against evals.md): every mixed-encoding "golden"/"edge"/"adversarial" case that claims a hit (E1-E3, E6, E11, E14, E18, E20-E23) asserts `exitCode === EXIT_HIT` or `result.status === 1` together with a positive hit-line count, so a gutted scanner that finds nothing would fail nearly all of them; the admit-side cases (E7, E12, E15, E17, E24, E25, E28) each assert the specific summary text expected, not merely "no throw"; the exit-code-precedence unit tests at the `scanFiles` level (E13 hit+undecodable -> HIT; E5/E17 scanned=0 or undecodable>0 -> CANNOT_RUN) correctly exercise `scanExitCode`'s stated ordering; and the CI-floor deny side (E29, E30, E32) each pins on `status` plus an `::error::` prefix plus `not.toContain(PASSED_LINE)`, so a report with a missing, short, or unreadable redaction suite cannot pass. Test counts in both files (44 and 9) match the counts claimed in evals.md's header, confirmed by counting `it`/`it.each` entries.
