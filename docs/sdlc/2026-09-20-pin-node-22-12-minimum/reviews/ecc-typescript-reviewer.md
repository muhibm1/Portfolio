Verdict: 4 findings (0 critical, 0 high, 3 medium, 1 low)

| Severity | File:line | Finding | Failure scenario | Fix |
|---|---|---|---|---|
| medium | scripts/check-npmrc.mjs:38-58 | `offendingLines` never strips a UTF-8 BOM before comparing lines to `ALLOWED_LINE` (confirmed by reading `readFileSync(npmrcPath, 'utf8')`, which does not strip a BOM, and `offendingLines`, which has no BOM handling) | A future edit to `.npmrc` made with an editor that writes "UTF-8 with BOM" (common Windows default in some editors) prepends U+FEFF to line 1; `engine-strict=true`'s comparison then fails, the guard reports line 1 as a violation with `key: null`, and the deploy workflow blocks on a functionally correct file | scripts/check-npmrc.mjs: strip a leading BOM before splitting, e.g. `text.replace(/^﻿/, '')`, matching the BOM guard the diff already added for `.nvmrc` in src/nodeVersionPin.test.js |
| medium | src/checkNpmrc.test.js:34 | `runCheckOn` calls `spawnSync(process.execPath, [scriptPath, directory], { encoding: 'utf8' })` with no `timeout`, unlike the sibling file src/nodeVersionPin.test.js which sets `SPAWN_TIMEOUT_MS` on every `spawnSync`/child npm call (confirmed by reading both files) | If `check-npmrc.mjs`'s `fs.readFileSync` ever blocks (e.g. the fixture path resolves to a network share, named pipe, or a directory under contention on a slow CI runner), this test hangs indefinitely instead of failing with a bounded timeout | src/checkNpmrc.test.js: add the same `timeout` option used in nodeVersionPin.test.js to `runCheckOn`'s spawnSync options |
| medium | src/nodeVersionPin.test.js:75-79 | `spawnNpm` falls back to `spawnSync('npm', args, { ...options, shell: true })` and relies solely on `options.timeout` to bound execution when `npm_execpath` is absent (believed, not verified: Node's `spawnSync` with `shell: true` on Windows spawns `cmd.exe`, and killing that process on timeout does not reliably terminate the grandchild process tree it launched, i.e. `npm.cmd` -> `node`) | On Windows, without `npm_execpath` set (running via `npx vitest` rather than `npm test`), a hung `npm ci` that hits the 20s timeout may leave an orphaned `node`/`npm` process holding a lock on the fixture directory; the following `afterEach`'s `fs.rmSync(fixtureDirectory, { recursive: true, force: true })` can then intermittently fail with `EBUSY`/`EPERM` on that runner | src/nodeVersionPin.test.js: when falling back to the shell path, use a process-group kill or `taskkill /pid /t /f` in a `try/finally` around the timed-out case, or document why the existing `npm_execpath` path (used under `npm test`, the project's documented test command) is the only one exercised in CI |
| low | src/nodeVersionPin.test.js:158-168 | The EBADENGINE-below-floor test asserts `expect(result.status).not.toBe(0)` rather than distinguishing a real non-zero exit from `status === null` (a killed/timed-out/signalled spawn, confirmed by Node's documented `spawnSync` semantics) | If this `npm ci` call hits the 20s timeout instead of completing, `result.status` is `null`, which satisfies `not.toBe(0)`; the test then fails only on the later `output.toContain('EBADENGINE')` assertion, producing a misleading "expected '' to contain 'EBADENGINE'" failure that obscures the real cause (a timeout, not an engine mismatch) | src/nodeVersionPin.test.js: assert `result.status !== null` (or check `result.signal`/`result.error` for `ETIMEDOUT`) before or alongside the EBADENGINE content check, so a timeout reports as a timeout |

Findings outside scope: docs/sdlc prose changes (CLAUDE.md, constraints.md, codebase-map.md, .workhorse/profile.yml) and the YAML-as-text assertions in src/deployWorkflowNodeVersion.test.js about permission-block counts are conformance/documentation concerns, not Node runtime correctness; not reviewed here.

Not verified: whether any editor actually used by this team writes a BOM into .npmrc (believed plausible, not observed in this repo's current file, which has no BOM); whether Windows `spawnSync(..., { shell: true, timeout })` in this Node version actually leaves orphaned processes (believed from documented Node/Windows process-tree behavior, not reproduced).
