Verdict: 3 findings (0 critical, 1 high, 1 medium, 1 low)

| Severity | File:line | Finding | Failure scenario | Fix |
|---|---|---|---|---|
| high | src/nodeVersionPin.test.js:1-250 | No test asserts the repository's own `node_modules` survives the fixture `npm ci` spawns (confirmed by reading the file; no such assertion exists) | Someone reverts `isNpmLifecycleKey` to case-sensitive, or drops the `npm_config_`/`npm_package_` prefix strip, on a Windows CI runner -> `NPM_CONFIG_LOCAL_PREFIX` leaks into a fixture spawn, `npm ci` targets the repo root again, and the repo's real `node_modules` is deleted, exactly as fixed in d5c0114 -> all 19 new tests still pass because none of them look at the host's own `node_modules`, so CI reports green while the working tree is destroyed | src/nodeVersionPin.test.js: add a `beforeAll`/`afterAll` (or per-test) check that snapshots `fs.readdirSync(path.join(REPOSITORY_ROOT, 'node_modules')).length` (or an mtime of a stable file such as `node_modules/.bin`) before the describe block's spawns and asserts it is unchanged after, per verification.md's manual check |
| medium | docs/sdlc/2026-09-20-pin-node-22-12-minimum/evals.md:38,41,42 | GC113 (doc corrections, R111), GC116 and GC117 (profile `sensitive_paths`/`tier_floor_paths`/control-key protection, R112, R115) are "commands run by the verifier," not tests in any of the three new files (confirmed: no reference to `.workhorse/profile.yml`, `codebase-map.md`, or `constraints.md` content in nodeVersionPin.test.js, checkNpmrc.test.js, or deployWorkflowNodeVersion.test.js) | A later change reverts `CLAUDE.md`/`docs/sdlc/codebase-map.md` to blame npm issue 4828 again, or drops `.npmrc`/`.nvmrc`/`scripts/check-npmrc.mjs` from `sensitive_paths`, or widens `protected_paths` -> `npm test` and CI both stay green; only a human re-running these exact `grep` commands during a future verification would notice, and nothing forces that | Add a Vitest spec (e.g. `src/nodePinDocsAndProfile.test.js`) that greps the four files for the GC113/GC116/GC117 strings, matching the pattern of `src/checkBuiltCssFonts.test.js` |
| low | src/deployWorkflowNodeVersion.test.js:83-115 | The permissions test locates blocks by line-order (`permissionsIndexes[0..2]`) and checks only the two lines immediately following each `permissions:` line, with no indentation/nesting check (believed, not verified — not reproduced) | A hand edit relocates the build job's `permissions:` block one indent level so it nests under a step instead of the job, while keeping file order and the literal `contents: read`/`pages: read` lines unchanged -> the test still passes because it never inspects leading whitespace or YAML structure, only text order | none proposed; would require either a minimal indentation-depth check (count leading spaces per block) or a real YAML parser, which the plan explicitly ruled out (ADR/spec Alternatives) |

Findings outside scope: none

Not verified:
- Whether the d5c0114 fixture-leak bug is actually reproducible today by reverting `isNpmLifecycleKey` (believed from reading the case-insensitive strip logic and the historical commit message, not re-triggered).
- Whether a real indentation-only corruption of `deploy.yml` would pass CI's YAML syntax step upstream of this test (not run).
- Confirmed by reading all three test files line by line: the 19 tests correctly implement their claimed eval IDs, none is tautological (each compares repo content to a literal expected value, not to itself), and each of the four named regression scenarios (delete `.npmrc`, flip `engine-strict` to false, hardcode `node-version: 22`, add a `registry=` line) fails a specific, correctly-reasoned test (GC109/AD23 in `nodeVersionPin.test.js`, `sets no node-version input` and `reads the Node version from .nvmrc` in `deployWorkflowNodeVersion.test.js`, respectively), plus the CI guard step (`scripts/check-npmrc.mjs`, run directly on the real `.npmrc`) independently rejects the deleted/tampered `.npmrc` cases before `npm ci` runs.
