Verdict: 3 findings (0 critical, 0 high, 1 medium, 2 low)

| Severity | File:line | Finding | Failure scenario | Fix |
|---|---|---|---|---|
| medium | scripts/check-test-floor.mjs:23-24 | The test-count floor has no per-file pin for the four new route-page suites (confirmed: `MINIMUM_SUITE_PASSED = 12` and one pinned file, `src/checkPhoneRedaction.test.js`, are the only two rules) | `src/routePaths.test.jsx`, `src/routePages.test.js`, `src/checkRoutePages.test.js` or `src/deployWorkflowRoutePages.test.js` is deleted, gutted, or a case is dropped -> the whole-suite total (well above 12 with the rest of the suite, confirmed by the 27 tests this change adds) still clears the floor -> CI stays green with the path-traversal guard test (FL... in routePages/routePaths), the log-hygiene tests (AD4/AD5), or the workflow-shape tests silently gone, exactly the "CI proves nothing" pattern the security baseline names | scripts/check-test-floor.mjs: add a pinned per-file floor entry for at least one of the new route-page suites (mirroring `REDACTION_SUITE_PATH`/`PINNED_REDACTION_PASSED_COUNT`), so removing it fails the build the same way removing the redaction suite does |
| low | scripts/check-route-pages.mjs:104-114 (`unexpectedOffenders`) | The stray-file scan only flags files literally named `index.html` (confirmed: `entry.name === 'index.html'` is the only filter) | A stray `dist/work/<x>.html` or any non-`index.html` HTML file lands in `dist/` (e.g. from a future build step, a merge artifact, or a hand-added file) -> GitHub Pages serves it with HTTP 200 at a real URL -> `check-route-pages.mjs` exits 0 and the deploy proceeds, so the "every unexpected page is caught" guarantee the design and R119 describe silently does not cover this case (this gap is also named as a low, open item in the spec's own constraint audit, row 5) | scripts/check-route-pages.mjs: extend `unexpectedOffenders` to flag any `*.html` file under the scanned tree that is not the expected shell, `404.html`, or a route page, not only ones named `index.html` |
| low | .github/workflows/deploy.yml (new "Smoke R121 and R122" step, `root_digest=$(sha256sum smoke/root.html \| cut -d ' ' -f 1)` and the two `if [ "$root_digest" != "$deep_link_digest" ]` / `"$unknown_digest"` comparisons) | The pass/fail of both new digest assertions depends entirely on GitHub Actions' implicit `bash -eo pipefail` default to turn a missing or unreadable `smoke/root.html` into a loud step failure; the step itself asserts nothing about `smoke/root.html` before comparing (believed, not verified: GitHub's documented default for a bare `run:` bash step is `-eo pipefail`, not independently confirmed against GitHub's docs in this session) | If `smoke/root.html` is ever empty or missing (an earlier step regresses, or a future edit runs this step under a shell without `-e`) `sha256sum` emits nothing useful and `$root_digest` becomes an empty string; if the same happens to `$deep_link_digest`/`$unknown_digest` (e.g. curl silently writes nothing to `-o` on some failure path) the empty-string comparison passes and the step prints PASS for a page that was never actually fetched correctly | .github/workflows/deploy.yml: after computing `root_digest`, assert `[ -n "$root_digest" ]` (or `[ -s smoke/root.html ]`) explicitly before using it, so the guarantee does not rest solely on the shell's implicit flags |

Findings outside scope: none.

Not verified: whether a `closeBundle` throw in `vite.config.js` (the case where `writeRoutePages` fails, e.g. R118's slug-validation error) actually fails `npm run build` was labelled "believed" in ADR 0001/0002/spec.md; I traced this in this session by reading `node_modules/rollup/dist/shared/rollup.js` (`hookParallel` awaits `Promise.all` over each plugin's hook, so a rejection propagates) and `node_modules/vite/dist/node/chunks/dep-BK3b2jBa.js:65721-65733` (`bundle.close()` runs unguarded inside the `finally` block of the build function, so a rejection there overrides a successful return) — this is confirmed to fail the build; not a defect, noted only because the spec's own label was more cautious than the code turned out to require.
