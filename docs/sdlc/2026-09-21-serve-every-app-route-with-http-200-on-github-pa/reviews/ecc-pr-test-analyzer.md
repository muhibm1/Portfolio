Verdict: 3 findings (0 critical, 0 high, 2 medium, 1 low)

| Severity | File:line | Finding | Failure scenario | Fix |
|---|---|---|---|---|
| medium | scripts/check-test-floor.mjs:25-26 | The whole-suite floor (12 passed) and the per-file pin (only `src/checkPhoneRedaction.test.js`) give the four new security-relevant test files no named protection (confirmed by reading the script) | A later edit deletes or renames `src/routePaths.test.jsx` (R118 slug-injection guard, FL1) or `src/routePages.test.js` (AD1 path-escape guard) -> `npm test`'s suite total stays well above 12 -> `check-test-floor.mjs` exits 0 -> CI is green with the security regression tests gone | scripts/check-test-floor.mjs: add a per-file pin for `src/routePaths.test.jsx` and `src/routePages.test.js` (or a lower-bound total that only holds if all four new files ran), mirroring the existing `REDACTION_SUITE_PATH` pattern |
| medium | src/checkRoutePages.test.js (no case) | No test drives `check-route-pages.mjs` against the fixture used by GC7 with an *extra* non-`index.html` HTML file (e.g. `work/x.html`), even though spec R119's unexpected-file rule (scripts/check-route-pages.mjs:96-107, confirmed) only walks for files literally named `index.html` | A future build step, or a stray copy step, leaves `dist/work/case-study.html` (any other filename) -> `unexpectedOffenders` never matches it (`entry.name === 'index.html'` filter, confirmed) -> the check exits 0 and Pages serves an unvalidated file at 200 | src/checkRoutePages.test.js: add a case writing a stray `*.html` file that is not named `index.html` under the fixture and assert the current behavior (exit 0, file not flagged), so the known gap the spec's own constraint audit already flagged (spec.md low finding, R119) is pinned by a test instead of silently drifting further from "nothing else answers 200" |
| low | src/routePages.test.js:69-72 (FL2) | The "writes nothing" assertion for the missing-shell case only checks `listFilesUnder(fixtureDirectory)` is empty, which is true here only because `writeShell()` was never called in that test, not because the writer stopped partway through a real write | Believed, not verified: if `writeRoutePages` validated shell existence only for the first target and wrote before checking later targets, this test would still pass since it never exercises "shell exists, but a later validation step fails after some writes happened" | src/routePages.test.js: add a case where the shell exists and a later path (not the first) in the list is the one that resolves outside `outDir`, then assert no route directories were created (the current implementation's separate map-then-loop order already satisfies this; the test would make that ordering a regression-checked contract instead of an implementation detail) |

Findings outside scope: none
Not verified: whether `dist/` can ever legitimately contain a non-`index.html`, non-`404.html` HTML file under normal Vite output (believed no, based on the spec's own "today `dist/` holds only `index.html` and `404.html`" note, not independently re-verified against a fresh build in this session)
