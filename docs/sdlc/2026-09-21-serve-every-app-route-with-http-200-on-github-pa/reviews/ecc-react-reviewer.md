Verdict: 2 findings (0 critical, 0 high, 2 medium, 0 low)

| Severity | File:line | Finding | Failure scenario | Fix |
|---|---|---|---|---|
| medium | src/routePaths.test.jsx:31-42 | GC2 renders `<App>` inside `MemoryRouter` at every path from `staticRoutePaths` and asserts a single level-1 heading, but nothing asserts the route actually resolved to the page a human would expect (only that it is not literally "Page not found") (confirmed, read the assertion). | A future regression that made `work/:slug` render `WorkIndexPage`'s heading instead of `CaseStudyPage`'s (e.g. a route-order bug) would still pass GC2, since `WorkIndexPage`'s h1 also is not "Page not found" (believed, not verified — did not check `WorkIndexPage`'s h1 text). | src/routePaths.test.jsx: assert against an expected heading text per route (or `data-testid`/`getByRole('main')` content check) rather than only excluding the not-found string. |
| medium | src/routePaths.test.jsx:20-67 | The new file duplicates route-rendering coverage that `src/routes.test.jsx` already owns (spec line 69 cites `src/routes.test.jsx:40` for the trailing-slash slug case) without stating the relationship, so two files can silently diverge on what "every route renders" means (confirmed: both files render `App` under `MemoryRouter`). | A reviewer edits `src/routes.test.jsx` to fix a route assertion but does not know `routePaths.test.jsx` makes an overlapping claim; the two drift and one becomes stale while still green. | src/routePaths.test.jsx: add a one-line comment cross-referencing `src/routes.test.jsx` and clarifying GC2's job is coverage-completeness (every path in the shared list renders something), not per-route content correctness, which the other file owns. |

Verified in this session: `npm test -- src/routePaths.test.jsx` passes 13/13 in 242ms with no `act()` warnings (confirmed). `npm run lint` (oxlint) is clean on the whole tree (confirmed); the project uses oxlint, not `eslint-plugin-react-hooks`/`jsx-a11y`, which is a pre-existing repo condition, not introduced by this diff (believed, not verified against prior reviews). `npm run build` then `node scripts/check-route-pages.mjs` writes 4 route pages and passes (confirmed). `src/App.jsx`, `src/components/CaseStudyPage.jsx`, and `src/pages/NotFoundPage.jsx` are unmodified by this diff (`git diff main...HEAD -- src/App.jsx src/components/CaseStudyPage.jsx src/pages/NotFoundPage.jsx` produced no output, confirmed) — the h1 GC2 depends on exists at `src/components/CaseStudyPage.jsx:86` (confirmed). `src/routePaths.js` is plain JS with no React import and no component/hook code (confirmed). `react-router` import paths in the new test file (`from 'react-router'`) match the package version and every other component/test in the repo (confirmed via grep). Vitest is configured with `globals: true` (`vite.config.js:42`), so Testing Library's auto-cleanup runs between the `it.each` iterations in GC2 without an explicit `afterEach(cleanup)` (confirmed).

Findings outside scope: `scripts/route-pages.mjs`, `scripts/check-route-pages.mjs`, `vite.config.js`'s `closeBundle` writer, and `.github/workflows/deploy.yml` are Node/build-script and CI concerns (path traversal guard, exit codes, curl redirect handling), not React component code — deferred to wh-bug-reviewer / wh-security-reviewer / typescript-reviewer lanes. `src/checkRoutePages.test.js`, `src/routePages.test.js`, and `src/deployWorkflowRoutePages.test.js` contain no React/JSX and are outside this agent's lane.

Not verified: whether `WorkIndexPage`'s or `HomePage`'s h1 text could coincidentally overlap with another route's h1 in a way that would mask the medium finding above — not read in this session. Whether oxlint's ruleset enforces any hooks-specific or jsx-a11y-equivalent rule was not independently confirmed beyond `npm run lint` exiting clean (oxlint's included rule set was not inspected).
