Verdict: 3 findings (0 critical, 0 high, 2 medium, 1 low)

| Severity | File:line | Finding | Failure scenario | Fix |
|---|---|---|---|---|
| medium | scripts/check-route-pages.mjs:97-107 | `unexpectedOffenders` only flags stray `index.html` files, not other extension-less or `.html` files under `dist/` (confirmed, matches spec's own open finding, constraint audit row on R119) | a stray `dist/work/other.html` or a case-insensitively duplicated `Index.html` (Windows build) lands in `dist/` and answers 200 for a crawler without the check catching it | scripts/check-route-pages.mjs: widen the scan to flag any `.html` file, or any file, outside the expected set rather than matching only literal `index.html` |
| medium | .github/workflows/deploy.yml:365 (curl `-L --max-redirs 2`) | The deep-link smoke fetch follows redirects to any scheme or host, so a hijacked or misconfigured DNS/Pages redirect to `http://` or an off-origin host would be followed silently and only fail if the body digest mismatches, not on the redirect itself (confirmed by reading the step; also flagged as open in spec's own constraint audit) | Pages or an intermediary starts redirecting `work/apple-llm-triage` to an attacker-controlled or downgraded origin that happens to serve byte-identical content | .github/workflows/deploy.yml: add `--proto-redir =https` (and optionally assert `url_effective` starts with `$ROOT_URL`) to the curl invocation at the R121 step |
| low | scripts/route-pages.mjs:33-41 | `writeRoutePages` validates every target path before writing (good), but the actual `fs.writeFileSync` loop has no per-file error handling, so a mid-loop OS-level failure (disk full, permission) leaves a partial set of route pages with no cleanup and a raw Node error surfaced instead of a `Route page check`-style message (believed, not verified — no fixture test exercises a write failure) | disk fills up after 2 of 4 route pages copy successfully during a real `vite build` | scripts/route-pages.mjs: wrap the write loop in try/catch and rethrow a message naming which target failed, or rely on the post-build check (R119) to catch the partial state, and note in a comment that the check step is the safety net for this case |

Findings outside scope: none

Not verified: the disk-full/partial-write scenario for scripts/route-pages.mjs:33-41 (believed, not verified — no test exercises it); live GitHub Pages redirect behavior for R121/R122 (spec itself defers this to the first post-merge deploy run).
